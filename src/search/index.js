"use strict";

const normalizeSearchText = require("./normalizeSearchText");
const makeSearchTokens = require("./makeSearchTokens");

module.exports = {
  normalizeSearchText,
  makeSearchTokens,
};
